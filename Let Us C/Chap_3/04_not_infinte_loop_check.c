#include<stdio.h>

int main(){

    // int i = 1 ; 
    // while ( 1 ) // Infinite-Loop as no end-condition or break used
    // i++ ; 

    // for ( ; ; ) ; // Infinite loop as no end-condition given

    int True = 0, false ;
    while ( True ) // Condition is True but since True's value is 0(False) so loop will not run
    { 
    false = 1 ; 
    } 

    // int y, x = 0 ;
    // do 
    // { 
    // y = x ; 
    // } while ( x == 0 ) ; // Infinite-Loop as condiion is always True and y value is being changed


    return 0;
}